<p style="color: green"><%= notice %></p>
<h3 class="title-create">Criar Conta</h3>

<% @users.each do |user| %>
  <%= render user %>
  <p>
    <%= link_to "Show this user", user %>
  </p>
<% end %>
</div>
<%= link_to "New user", new_user_path %>
